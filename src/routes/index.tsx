import { Routes, Route } from 'react-router-dom';

function AppRoutes() {
  return (
    <Routes>
      <Route path="/" element={<div>School Management SaaS</div>} />
    </Routes>
  );
}

export default AppRoutes;
